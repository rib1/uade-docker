name: Code Quality Checks

on:
  push:
    branches:
      - main
    paths:
      - 'web/**/*.py'
      - 'web/**/*.js'
      - 'web/**/*.css'
      - '.agents/AGENTS.md'
      - '.github/workflows/*.yml'
      - '.agents/README.md'
      - '.agents/project-lessons.md'
      - 'Dockerfile*'
      - 'docker-compose*.yml'
      - 'docs/**/*.md'
      - 'test/Dockerfile*'
      - 'test/docker-compose*.yml'
      - 'test/check-instructions.mjs'
      - 'test/check-purgecss.mjs'
      - 'test/check-playwright-version-sync.mjs'
      - 'test/check-code-quality.sh'
      - 'test/check-code-quality.ps1'
      - 'test/knip.config.js'
      - 'test/purgecss.config.js'
      - 'test/Dockerfile.quality'
      - 'test/docker-compose.quality.yml'
      - 'test/docker-compose.tooling.yml'
      - 'test/package.json'
      - 'test/requirements-quality.txt'
  pull_request:
    branches:
      - main
    paths:
      - 'web/**/*.py'
      - 'web/**/*.js'
      - 'web/**/*.css'
      - '.agents/AGENTS.md'
      - '.github/workflows/*.yml'
      - '.agents/README.md'
      - '.agents/project-lessons.md'
      - 'Dockerfile*'
      - 'docker-compose*.yml'
      - 'docs/**/*.md'
      - 'test/Dockerfile*'
      - 'test/docker-compose*.yml'
      - 'test/check-instructions.mjs'
      - 'test/check-purgecss.mjs'
      - 'test/check-playwright-version-sync.mjs'
      - 'test/check-code-quality.sh'
      - 'test/check-code-quality.ps1'
      - 'test/knip.config.js'
      - 'test/purgecss.config.js'
      - 'test/Dockerfile.quality'
      - 'test/docker-compose.quality.yml'
      - 'test/docker-compose.tooling.yml'
      - 'test/package.json'
      - 'test/requirements-quality.txt'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  code-quality:
    name: Run Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Run code quality checks
        run: |
          docker compose -f docker-compose.yml -f test/docker-compose.quality.yml up --build quality-check

      - name: Check exit code
        if: failure()
        run: |
          printf "❌ Code quality checks failed!\n"
          printf "Please run './test/check-code-quality.sh --fix' (Bash) or '.\test\check-code-quality.ps1 -Fix' (PowerShell) locally to auto-fix issues.\n"
          exit 1
